# 阶段 0 冒烟测试报告（2026-07-20）

环境：系统 Node 24（`C:\Program Files\nodejs\node.exe`，better-sqlite3 预编译 ABI 137），启动命令
`node node_modules/tsx/dist/cli.mjs src/apps/answer-card/server/index.ts`（端口 5174，RBAC 关闭=开放模式）。

> ⚠️ 默认 `node` 为 v22，ABI 不匹配会 DLOPEN 失败；用默认 Node22 跑 `npm run server` 需先 `npm rebuild better-sqlite3`。

## 结果

| # | 请求 | 期望 | 实际 | 结论 |
|---|------|------|------|------|
| 1 | GET /api/app/health | 200 | 200 `{ok:true,db...}` | ✅ 重复 health 已删，仅一处 |
| 2 | **GET /api/exams/18/assigned-formula** | 200 正确结构 | 200 `{formula,isAssignedSubject,presets:[3项]}` | ✅ **P0 实锤修复（此前 404）** |
| 3 | PUT /api/exams/18/assigned-formula `{formula,recalculate:false}` | 200 | 200 `{ok:true}` | ✅ 保存生效 |
| 4 | GET /api/exams/999999/assigned-formula | 404 优雅 | 404 "考试不存在" | ✅ |
| 5 | GET /api/exams/abc/assigned-formula | 400 不挂死 | 400 "非法考试 ID" | ✅ async 包装下返回 JSON，无挂死 |
| 6 | PUT /api/exams/18/answers `{}` | 400 | 400 "未提供答案修改数据" | ✅ 0.4 校验生效 |
| 7 | PUT /api/exams/18/answers `{"answers":{"abc":["A"]}}` | 400 | 400 "题号非法: abc" | ✅ |
| 8 | PUT /api/exams/18/answers `{"answers":{"1":"x"}}` | 400 | 400 "题号 1 的答案必须是字符串数组" | ✅ |

## 结论
- 阶段 0 四项修复**运行时验证通过**：赋分公式路由复活（核心 P0）、async 错误包装生效（异常→JSON 而非挂死）、鉴权语义统一、score-editing 入参校验生效。
- server 日志无崩溃 / 未捕获异常。
- 测试对 DB 的改动（exam 18 assigned_formula）已还原为 NULL。

## 遗留 / 可选
- asyncHandler「未捕获 rejection→500」路径仅代码级 + 存活验证，未做强制抛错注入测试（怕污染数据，故未做）。如需 100% 证明可临时在 handler 内 `throw new Error` 再请求确认返回 500 JSON。
- 下次跑服务注意 Node 版本（见顶部 ⚠️）。
