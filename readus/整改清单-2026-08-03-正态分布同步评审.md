# 整改清单：正态分布同步三 PR 评审（#203 / #205 / #204）

> 评审日期：2026-08-03
> 评审对象：#203 正态分布同步（含 v1.10.0 分析增强）→ #205 全量回退 → #204 重放并修复审计发现
> 状态：待拆分整改，建议按下述 5 个 PR 分别落地

## 背景结论

- 功能方向合理（难度 P / 区分度 D / 总体分析 / 大考 AI），但 #203 以空 body 的 6k 行单 PR 合入，4 分钟后被整体回退，再 5 分钟后由 #204 重放 + 46 行修复，全程无有效人工评审窗口。
- 合并前未发现的缺陷（大考总体分析前端请求路径 404、AD p 值公式错误、班级筛选崩溃、大考参与口径错误）证明现有验证没有覆盖关键路径，且验证脚本未接入自动化。

## 一、建议拆分成的整改 PR

### PR-A：统计正确性（P0）

| 编号 | 问题 | 现状 | 改法 | 验证 |
| --- | --- | --- | --- | --- |
| A1 | 考试级区分度 D 口径不一致 | TS `getExamMetrics` = 各题 D 的算术平均；Python `get_exam_overview` = 总分极端组差/满分 | 统一口径（建议：考试级 D 用逐题 D 均值，AI 工具同口径），并在文档写明定义 | 同一考试 Web 与 AI 返回的 D 一致；合成数据断言 |
| A2 | `normality()` 注释与实现不符 | 注释“任一检验 p≥0.05 即通过”，实现仅看 Shapiro-Francia | 注释改为与实现一致；或按注释实现 | typecheck + 单测 |
| A3 | KS p 值使用 Smirnov 渐近式 | 参数由样本估计，p 值偏乐观 | 加 Lilliefors 修正或蒙特卡洛；至少在前端标注“参考值” | 正态/偏态合成数据 p 值对比 scipy |
| A4 | 小样本 D/P 展示 | n<4 时极端组仅 1 人，D 噪声大 | 前端 n<阈值（如 4）时不展示 D，只显示“样本不足” | 合成 2-3 人数据冒烟 |

### PR-B：API 健壮性与安全（P1）

| 编号 | 问题 | 现状 | 改法 | 验证 |
| --- | --- | --- | --- | --- |
| B1 | 新端点参数未校验 | `question-students` 有校验；`distribution`/`metrics`（考试+大考）均无 | 统一抽一个校验中间件：examId/groupId 正整数、classId ≥0、mode 白名单 | 传 NaN/负数/字符串返回 400 的用例 |
| B2 | 内部错误原文泄露 | 大考新端点 `res.status(500).json({message: error.message})` | 统一 `next(error)`，由中央错误处理返回脱敏信息 | verify:security-critical + 手工 500 用例 |

### PR-C：测试落地（P1）

| 编号 | 问题 | 现状 | 改法 | 验证 |
| --- | --- | --- | --- | --- |
| C1 | 验证脚本未接入 npm | `scripts/bugfix-analysis-verification.ts` 不在任何 npm script | package.json 增加 `"verify:analysis": "tsx scripts/bugfix-analysis-verification.ts"` | 一键可跑、可进 CI |
| C2 | 统计库无单测 | `src/shared/stats.ts` 纯函数零测试 | 补 `node:test`/vitest 单测（难度/区分度/直方图/正态性/分位数） | `npm test` 全绿 |
| C3 | E2E 覆盖不足 | #203 的大考总体分析 404 未被发现 | 冒烟增加前端路由断言：`/api/exam-groups/:id/distribution` 200 | 真实考试 26/大考 8 冒烟 |

### PR-D：工程清理（P2）

| 编号 | 问题 | 现状 | 改法 | 验证 |
| --- | --- | --- | --- | --- |
| D1 | `design/` 范围蔓延 | 3.4k 行设计系统 demo，`src/` 零引用 | 移入独立 PR/仓库，或先从 main 移除 | `rg "design/" src` 为空 |
| D2 | 版本号漂移 | package.json 1.9.6、tag V1.9.6、CHANGELOG v1.10.0.3 | 对齐三者（建议以 CHANGELOG 为准升版） | `npm version` 与 tag 一致 |
| D3 | CHANGELOG 记录本次整改 | — | 新增条目引用本清单与相关 PR | 文档审阅 |

### PR-E：流程规范（P2，文档）

| 编号 | 问题 | 建议 |
| --- | --- | --- |
| E1 | 大 PR 无描述无评审 | 仓库加 PR 模板：背景/影响面/测试输出/冒烟截图 |
| E2 | 合入后立即回退-重放 | 禁止未经讨论的“回退后原样重放”；回退后先开 issue 分析根因再重开 PR |
| E3 | 单 PR 过大 | 约定阈值（如 >20 文件或 >1000 行）必须拆 PR |

## 二、GitHub Issue 建议

1. **bug**：考试级区分度 D 网页与 AI 报告口径不一致（对应 A1）
2. **bug**：分析新端点参数校验缺失、内部错误泄露（对应 B1/B2）
3. **测试**：分析验证脚本未接入 npm/CI（对应 C1/C2/C3）
4. **流程**：大 PR 拆分与评审规范（对应 E1-E3）
5. **技术债**：design/ 设计系统未接入源码（对应 D1）

## 三、关键位置索引

- `src/shared/stats.ts:86` 极端组区分度；`:250` KS；`:277` AD；`:305` normality
- `src/server/repositories/AnalysisRepository.ts:381` 考试级 D（逐题均值）；`:397` 逐题下钻
- `src/apps/answer-card/server/routes/analysis.ts:327/355/367` 考试侧新端点
- `src/server/routes/exam-groups.ts:505/529/554` 大考侧新端点（错误处理直出 error.message）
- `llmclient/tools/grades.py:157` Python 考试级 D（总分极端组）
- `scripts/bugfix-analysis-verification.ts` 未接入 npm scripts
- `src/apps/answer-card/client/components/AnalysisOverall.tsx:31` 已修复的大考分布路径
