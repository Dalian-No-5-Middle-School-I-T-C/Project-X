# 本机验证结果

2026 年 9 月 6 日，在本机 Windows + Ubuntu WSL 实际运行完成。

| 对照 | 源码 | 结果 |
|---|---|---|
| 旧版主分支 | `70697163f9c456677f8ad4499b65b832e9032810` | 20 个 B 类用例全部复现失败，分属 16 个根因族 |
| 已修复版本 | `62def7aaea3cdb818ccf499418910ea5ed348642` 工作区快照 | 28/28 项通过：20 个缺陷用例、8 个链路检查 |

两次正式结果的测试工具哈希一致：`feb66f94e8f3841631a1f5e71c5cd2c34d22f3ce86714164dc5f719eea2e587d`。候选测试通过 `run.ps1` 一键入口启动，评分器返回 20/20 用例修复、16/16 根因族修复、8/8 链路通过、0 回归。

正式本机结果目录：

- `results/baseline-1788675211404/`
- `results/candidate-1788675468922/`

便携 ZIP 中附带对应的 `evidence/baseline/` 和 `evidence/candidate/` 精简证据；不包含完整源码快照、依赖、数据库备份或任何真实 API Key。结果 JSON 保留原运行路径以便追溯，不要求解压到该路径。

## 关键证据

- 设置、阈值、草稿、题块配置更新和人工改分：基线出现 MariaDB `Incorrect datetime value`，候选保存并读回成功。题块配置必须先新增、再修改才能触发基线缺陷。
- 联考和 AI 查询：基线分别出现派生表 SQL、`CAST ... AS REAL`、参数名不匹配和 `_connect` 未定义等实际错误，候选查询通过。
- 150 分制夹具成绩为 59.5、75、90、150。基线给出及格率 75%、优秀率 50%；候选为正确的 50%、25%，分布总人数为 4。
- 基线分析页面出现 `"bar" is not a registered controller`；候选绘制图表成功，截图随包附带。
- 本机 C++ 对四份生成图片均成功识别。基线没有返回题块图片，且远程上传完成后成绩列表为空；候选服务器持久化 20、15、10、5 分，重复完成操作通过。
- 浏览器实际配置本机扫描端连接 WSL，导入四张图片并点击“开始识别判分”。候选页面显示上传完成，服务器得到相同的四个成绩；基线页面流程虽完成上传，服务器仍无成绩。
- 候选 AI 任务通过真实 Python SDK 请求本地服务商夹具，实际调用 `get_exam_overview` 工具并以 `done` 状态保存非空报告。
- 考试发布、CSV 导出、学生查分、班级对比和 MariaDB ZIP 备份均通过。

F05（AI 任务）和 F06（发布查分）在基线被前置缺陷阻塞，未计入 20 项缺陷数。F08 的前端上传链路另有失败，也未重复加入 B 类缺陷计数。

测试使用生成图片与真实 Windows 原生识别器，没有物理扫描仪走纸；AI 使用确定性本地服务商夹具，没有评价外部模型质量，也没有进行网络安全测试。首次安装依赖耗时较长，后续运行使用本机独立缓存。
